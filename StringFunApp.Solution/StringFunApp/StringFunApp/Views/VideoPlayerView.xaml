<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:StringFunApp"
             xmlns:video="clr-namespace:FormsVideoLibrary"
             x:Class="StringFunApp.Views.VideoPlayerView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <video:VideoPlayer x:Name="videoPlayer" Grid.Row="0" 
                           Source="{Binding Source={x:Reference StapVideos},
                                            Path=SelectedItem.VideoSource}"></video:VideoPlayer>

        <ListView x:Name="StapVideos" Grid.Row="1" ItemsSource="{Binding Videos}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding DisplayName}"></Label>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>
